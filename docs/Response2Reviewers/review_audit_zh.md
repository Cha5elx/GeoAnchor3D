> **后续信息更新：** 用户已确认旧门控消融使用两数据集训练，新消融使用四数据集联合训练；效率比较输入与配置相同。下文相关疑问已有解释，不再作为未澄清的事实问题。多种子结果仍待提供。九条中英文回复已同步完善，当前建议见同目录 `revision_notes_zh.md`。以下保留首次审查记录以便追溯。

# 回复信二次检查：审稿人视角

检查日期：2026-09-11。

检查对象：当前三个英文回复文件及三个中文阅读版，以及用户指定的最新 `SGR/docs/GeoAnchor3D.tex`。这是一轮文本、证据范围及数值一致性检查，没有重新运行实验，也没有验证原始实验日志或参考文献全文。以下“可能追问”是审稿风险判断，不代表审稿人一定会提出这些问题。

## 总体判断

回复已经比初稿简洁，中英文对应的主要技术含义与实验数值一致，重复问题的回答也有不同重点。主要风险不在语言长短，而在部分原始实验要求尚未完成，以及回复与论文之间仍存在证据强度差异。不能据此保证审稿人不再追问。

本轮仅同步了 Reviewer 3 Response 4 的中英文：确认论文已将倒 U 型描述替换为“performance varies across the selected layer intervals”，删除过时的待修改说明，保留未提供逐区间方差与跨主干验证的事实。没有修改论文；Reviewer 2 Response 1 按用户此前要求保持原回答。

## 优先处理的问题

### 1. 多随机种子及超参数问题仍未回答

涉及 R1.5、R3.5，以及 R3.4 中的层区间稳定性。

- 两条统计稳定性回复仍为待补标记；论文没有相应均值和标准差结果。
- 仅报告完整模型的多种子结果，不能直接解决每个层区间排序的方差问题。
- 若要声称相对基线的提升超出了优化波动，还需交代基线结果来源以及是否进行了可比的重复运行，不能把一个基线点估计当作基线方差。
- R1.5 另问 LoRA 秩、学习率调度和损失权重是否调优。当前实现细节给出了数值，但没有说明选择过程，也没有敏感性结果。

可能追问：“这些提升是否只是随机波动？哪些参数被调过，是否公平？”

处理：保留待补状态，待真实结果和参数选择记录齐备再回答。不能用措辞修饰替代结果，也不应未经确认声称所有参数直接沿用基线。

### 2. 旧、新消融表的结果差异需要解释实验设置

涉及 R2.3、R3.2，并影响 R3.4。

论文中的数值分别为：

| 位置 | 配置 | ScanRefer Acc@0.5 | ScanQA CIDEr |
| --- | --- | --- | --- |
| `tab:gating_ablation`，第 459 行 | Learnable gating | 50.7 | 87.9 |
| `tab:layers_ablation`，第 525 行 | Layers 17–24 | 50.7 | 87.9 |
| `tab:gating_granularity_prior`，第 500 行起 | Full per-head | 51.1 | 89.9 |
| `tab:final_ablation`，第 435 行 | IGGA + GATH | 51.1 | 89.9 |

这些差异不必然意味着结果错误，但当前正文没有清楚说明两组设置的不同。“消融在 ScanRefer 和 ScanQA 上进行”也不能单独说明究竟是训练任务、评估任务还是其他配置不同。

可能追问：“同样采用可学习逐头门控和中间层，为什么结果不同？哪些其他设置改变了？”

最小处理：根据实际实验记录，在旧消融表附近补一句具体的训练任务、数据、配置或检查点区别。尚未确认原因，因此本轮没有自行补写解释，也没有改动数字。

### 3. Reviewer 2 Response 1 的完成声明仍超出论文实际修改

该回复按用户要求保留。

- 论文已经为 Q/K/V、语义注意力和有效空间掩码添加头索引，并说明空间矩阵共享。
- 第 240 行仍使用没有头索引的 fused attention 表达式和未定义的 `Normalize`。
- 第 243–249 行没有显式写出多头输出拼接。
- 第 203、208 行采用对象 j 的特征，而第 212 行解释的是对象 i 的特征。

可能追问：“回复说已经定义归一化和多头聚合，请问具体在哪里？”

这是可直接对照发现的文本不一致。保留原回答是用户已明确的选择，但不能因此将本条判定为已经完全解决。

### 4. 效率回答有数据，但测量协议仍可能被追问

涉及 R1.3、R3.8；论文第 722–757 行。

已明确硬件、批大小、FlashAttention-2、固定生成 32 个 token，以及训练耗时不含数据传输和优化器更新，这些信息有用。回复也没有把 GATH 无推理开销扩展为整个框架无计算开销。

但是，延迟由 2491.18 ms 降至 1993.78 ms，快约 20%。新增模块并不逻辑上排除实测加速，但这一幅度会促使审稿人检查比较设置。固定输出 token 数并不能单独说明输入计算量及其他运行设置完全一致。

可能追问：是否使用同一批输入和相同输入 token 长度？精度、KV cache、计时边界、预热次数、重复次数、GPU 同步方式是否一致？统计波动如何？

处理：优先补充已有测量记录中的真实协议。如果确有设置差异，应交代；如果不能确认，则不把加速作为算法优势。当前回复的谨慎限定值得保留。R1.3 已明确报告的是总体开销，尚未分别量化各模块，因此仍属于部分回应原请求。

### 5. 线性探测回应了几何信息问题，但不等同于证明 GATH 保持拓扑

涉及 R3.3、R3.10；论文第 709–713、811 行。

- 坐标探测支持完整 GeoAnchor3D 的坐标线性可恢复性更好。
- 完整模型与基线的对比，不能单独把全部差异归因于 GATH。
- 坐标回归并非全局距离或邻域保持指标；各层数值也不严格单调。
- 回复已经限定上述范围，但论文第 811 行仍从 t-SNE 聚类推向 GATH 的几何保持作用；引言、摘要中的渐进几何遗忘也比这组探测结果本身更强。

可能追问：“你证明的是更容易预测坐标，为什么结论仍是 GATH 防止拓扑遗忘？”

最小处理：将可视化段落的结论明确限定为定性补充，并与框架级线性可恢复性证据衔接。若坚持 GATH 的独立因果结论，才需要更直接的组件对照；不应声称现有实验已经提供了这种证据。

另一个可复现性信息缺口：论文说明了探测训练/测试的场景划分，但没有明确这些场景与主模型训练场景的关系，也未完整报告探测器优化设置。这里只记录缺少说明，不据此推断存在数据泄漏。

### 6. 删除“倒 U”完成了最小修改，但没有回答方差请求

涉及 R3.4；最新论文第 596 行。

已经完成的修改是真实、可核对的，因此本轮已同步回复。仍保留的“early layers underperform due to …”“most favorable trade-off”等句子包含机制解释或较强判断；层区间表本身只给出任务指标，其中早层的 BLEU-4 反而最高。

当前回复对此进行了限定：17–24 层在两项所列指标上最好，但不代表所有指标最好，也不建立统计显著性或普遍最优性。是否继续修改论文，遵循用户此前的最小修改要求，本轮未扩展处理。

## 原请求尚未完全覆盖，但可以坦诚限定范围的部分

- **R1.1：手工编码与可学习嵌入比较。** 当前回答解释了贡献归属及设计理由，但没有比较实验，不能证明手工编码更好。审稿人仍可能坚持原要求。
- **R1.2 / R3.9：候选质量。** 相同 Mask3D 候选支持受控比较，不等于质量敏感性测试。当前局限性说明准确，但不替代所请求的实验。
- **R1.4：其他 LLM 主干。** 收窄范围是诚实处理，但没有提供审稿人要求的实证迁移结果。
- **R3.2：逐层门控。** 已有逐头与共享标量的直接对照；逐层门控未测，当前解释交代了架构差异。是否接受这一范围，由审稿人判断。
- **R2.2 / R3.1：指令级路由。** 消融与任务内分组回应了两项关键请求，但组间差异只是关联，不能排除其他指令属性的作用，更不能单独证明模型理解了空间复杂度。当前回复已用审慎语言；论文中关于梯度冲突、任务先验被覆盖的机制说明，不宜被写成已由这些实验验证。

这些不是通过同义改写就能消除的问题。也不建议把每个潜在追问都塞进回复，导致回复重新变长；应在相关论文位置补足必要信息，并让回复简短指向那里。

## 21 条回复检查结果

“较完整”指当前回复与材料能够较好回应这条问题，不是接受保证。

| 条目 | 判断 | 说明 |
| --- | --- | --- |
| R1.1 | 部分回应 | 贡献与设计理由清楚，嵌入对照缺失 |
| R1.2 | 部分回应 | 明确候选限制，无敏感性实验 |
| R1.3 | 有定量回应 | 总体开销已报，分项开销及测量细节不足 |
| R1.4 | 限定范围 | 无跨主干实验 |
| R1.5 | 待补 | 多种子及超参数问题未完成 |
| R2.1 | 文稿不一致 | 按用户要求保留，归一化等完成声明仍需对应 |
| R2.2 | 基本回应 | 区分机制与监督，注意解释不能超过证据 |
| R2.3 | 有直接对照 | 需解释旧、新表设置差异 |
| R2.4 | 较完整 | 已交代继承部分及新增模块 |
| R2.5 | 较完整 | 结论与局限性已限定场景和任务范围 |
| R3.1 | 有直接分析 | 已做无先验消融与任务内分析，未证实因果机制 |
| R3.2 | 部分回应 | 标量对照已做，逐层未测，旧、新表需说明 |
| R3.3 | 有定量证据 | 支持框架级可恢复性，不隔离 GATH |
| R3.4 | 部分回应 | 已同步删去倒 U 表述，逐区间方差仍缺 |
| R3.5 | 待补 | 多种子结果未完成 |
| R3.6 | 范围已澄清 | 不再声明未见任务泛化，无留出任务实验 |
| R3.7 | 范围已澄清 | 不再声明跨域泛化，无新域实验 |
| R3.8 | 有定量回应 | 推理耗时的测量细节需要解释 |
| R3.9 | 部分回应 | 已说明候选失效边界，无鲁棒性实验 |
| R3.10 | 部分回应 | 定量证据已有，论文可视化措辞仍需匹配 |
| R3.11 | 文本请求已回应 | 当前论文加入了三条引用；本轮未核查文献全文或 bib 元数据 |

## 中文与提交版本

中英文回复的实验小数数值及条目对应已核对；没有发现需要改变科学含义的翻译错误。中文保留的审慎说明不是新的实验结论。

R3.1 的审稿意见原文存在公式缺失，当前仍保留来源备注，不能凭推测补成审稿人原话。两个“回复待补”及 R3.10 的编辑备注属于工作稿标记，不是正式给审稿人的最终文本，后续完成时再清理。

本轮没有用删除局限、隐藏不利指标或编造“已完成修改”的方式降低追问风险。
